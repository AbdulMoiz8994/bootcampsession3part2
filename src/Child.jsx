import React from 'react'

export const Child = (props) => {
    // console.log(props)
    return (
        <div>
        <h1>The Counter's Variable value= {props.counter}</h1>         
        </div>
    )
}
